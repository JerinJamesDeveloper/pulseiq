{
  "name": "pulseiq-api",
  "version": "1.0.0",
  "description": "PulseIQ REST API with MySQL",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:migrate": "mysql -u root -p < database_schema.sql",
    "db:seed": "node scripts/seed.js",
    "sync:process": "node scripts/processSync.js",
    "sync:clean": "node scripts/cleanSync.js"
  },
  "dependencies": {
    "@openrouter/sdk": "^0.9.11",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "helmet": "^7.0.0",
    "morgan": "^1.10.0",
    "mysql2": "^3.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
